name: harbinger
services:
  parser:
    image: harbinger:${APP_VERSION:-local}
    command: ["python", "-m", "workspace.parser_service", "/parser-queue"]
    user: "${APP_UID:-1000}:${APP_GID:-1000}"
    network_mode: none
    volumes:
      - ./state/parser-queue:/parser-queue
    read_only: true
    tmpfs:
      - /tmp:rw,noexec,nosuid,nodev,size=64m
    cap_drop: [ALL]
    security_opt: [no-new-privileges:true]
    pids_limit: 32
    mem_limit: 1152m
    init: true
    restart: "no"
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: harbinger:${APP_VERSION:-local}
    command: ["python", "-m", "workspace.cli", "--workspace", "/state", "serve", "--bind", "0.0.0.0"]
    user: "${APP_UID:-1000}:${APP_GID:-1000}"
    depends_on:
      parser:
        condition: service_started
    environment:
      APP_BIND: 0.0.0.0
      APP_PORT: "${APP_PORT:-8710}"
      APP_TLS_CERT: "${APP_TLS_CERT:-}"
      APP_TLS_KEY: "${APP_TLS_KEY:-}"
      APP_PEER_CA_BUNDLE: "${APP_PEER_CA_BUNDLE:-}"
      CONTAINERIZED: "1"
      PARSER_QUEUE: /parser-queue
    ports:
      - "${HOST_BIND:-127.0.0.1}:${APP_PORT:-8710}:${APP_PORT:-8710}"
    networks:
      team:
        aliases: [harbinger]
    volumes:
      - ./state:/state
      - ./state/parser-queue:/parser-queue
      - ./tls:/tls:ro
    read_only: true
    tmpfs:
      - /tmp:rw,noexec,nosuid,nodev,size=64m
    cap_drop: [ALL]
    security_opt: [no-new-privileges:true]
    pids_limit: 128
    mem_limit: 1536m
    init: true
    restart: "no"
    healthcheck:
      test: ["CMD", "python", "-c", "import os,socket;s=socket.create_connection(('127.0.0.1',int(os.environ['APP_PORT'])),2);s.close()"]
      interval: 5s
      timeout: 3s
      retries: 12
      start_period: 10s
networks:
  team:
    name: cptc-team-workspace
    external: true
